"use client";

import { DataNotification } from "@/interfaces/notification.interface";

interface Props {
  dataNotification: DataNotification
}

const ReplyNotification = ({ dataNotification }: Props) => {
  return (
    <div>Reply Notifications</div>
  )
}

export default ReplyNotification